import { StyleSheet } from "react-native";

export const PositionTypes = StyleSheet.create({
  // Position Types
  posRelative: {
    position: "relative",
  },
  posAbsolute: {
    position: "absolute",
  },
});
